# FlipZero — аудит и QA

Дата проверки: 23 сентября 2026. Репозиторий: `feat/superup-completion` (рабочая ветка уже содержит локальные коммиты и расходится с `origin/main`; синхронизация не выполнялась). Аудит основан на статическом просмотре приложения и безопасных локальных проверках.

## Найденные проблемы

| Проблема | Где | Severity | Как исправить / статус |
|---|---|---|---|
| Локальные тестовые credentials есть, но нет подключения к БД | окружение QA | high | В `.env.local` заданы `TEST_EMAIL` и `TEST_PASSWORD`; `DATABASE_URL` отсутствует. Попытка входа на локальном сервере вернула HTTP 500. Для приватного QA нужна изолированная тестовая БД и серверные параметры. Значения пароля не выводились. |
| Локальная ветка расходится с origin и содержит собственные коммиты | Git | high | Перед публикацией сверить ветки и разрешить расхождение; во время аудита не выполнять pull/rebase/push. |
| Голосовой токен игнорировал разрешения канала и выдавал право публикации любого медиа | `app/api/v1/channels/[channelId]/voice-token/route.ts` | critical | Исправлено: проверяются ViewChannels/ConnectVoice, SpeakVoice и Stream ограничивают источники публикации. Уже выданные токены действуют до истечения срока; для немедленного отзыва нужны средства LiveKit. |
| Менеджер ролей мог создать высшую роль, выдать себе недоступные права и изменить роли равных/старших | `app/api/v1/spaces/[spaceId]/roles/route.ts` | critical | Исправлено: проверка позиции и подмножества прав при создании/изменении, запрет выдавать Administrator менеджером, иерархия на изменение/удаление. Добавлена проверка источника мутаций. |
| Менеджер ролей мог снять права равного/старшего и назначить роли несуществующему участнику | `app/api/v1/spaces/[spaceId]/members/route.ts` | high | Исправлено: проверка членства и текущей высшей роли адресата до изменения, проверка источника PATCH/DELETE. Требуется интеграционная проверка при доступной БД. |
| У приложения Tauri было `csp: null` | `src-tauri/tauri.conf.json` | high | Добавлены базовые директивы `object-src`, `base-uri`, `frame-ancestors`. Полную политику `script-src`/`connect-src` нужно настроить после проверки удалённого Next.js и LiveKit в Windows WebView. |
| Доступна только сборка Windows, хотя Tauri updater/signature и другие платформы не настроены | `src-tauri/tauri.conf.json`, `/download` | medium | Явно указывать поддерживаемые платформы; публикацию macOS/Linux/Android планировать отдельно. Windows оставлен единственной desktop платформой. |
| Ссылка на выпуск использовала фиксированное имя установщика | `app/download/page.tsx` | high | Исправлено: GitHub Releases API с часовым кешем, выбор Windows `.exe`, версия/размер/SHA256 из метаданных, сообщение при отсутствии выпуска. Выпуск не удалось проверить с этой машины из-за сетевого ограничения. |
| На публичной странице разработчиков не раскрывались доступные API | `app/developers/page.tsx` | medium | Добавлены реальный метод чтения, Bearer-токен и scopes со ссылкой на документацию. Численные лимиты API в коде не закреплены: публикация чисел требует решения владельца. |
| Продуктовый стиль «ты/вы» смешан на лендинге | `app/page.tsx` | low | Владелец просил выбрать стиль; для комьюнити-продукта выбран «ты» на главном экране. Полный аудит копирайта по всем приватным разделам требует отдельной редакторской проверки. |
| JSON-LD утверждал поддержку macOS, Linux и Android, хотя публичная загрузка только для Windows | `app/page.tsx` | medium | JSON-LD исправлен на Windows и Web. |
| Общие метаданные задавали canonical `/`, из-за чего страницы без своих metadata могли наследовать корневой URL | `app/layout.tsx` | high | Удалён canonical/url из общих metadata, добавлены отдельные canonical/og:url для главной, download, developers и юридических страниц. Динамические и остальные страницы требуют дальнейшей ревизии. |
| Страницы входа и регистрации наследовали индексируемые robots metadata | `app/login`, `app/register` | medium | Добавлены `noindex`, canonical и отдельная metadata для входа. `/app` уже имел `noindex`. |
| В футере отсутствовали ссылки на документы и контакты | `app/page.tsx` | low | Добавлен футер со ссылками на privacy, terms и GitHub Issues для обратной связи. |
| Демо-чат показывал вымышленные счётчики как реальные данные | `app/page.tsx` | medium | Убраны числовые счётчики иллюстрации; демо скрыто от скринридера, заменены символы rail на SVG. Глобальных заявленных метрик на текущей странице нет. |
| Переключатель `RU` не работал | `app/page.tsx` | low | Удалён индикатор, похожий на переключатель; отдельную локализацию не добавляли. |
| Для неизвестного URL и падения рендера не было собственных экранов | `app/` | medium | Добавлены 404, error boundary с повтором и состояние загрузки `/app`. |
| Регистрация не ограничивала частоту запросов на сервере | `app/api/v1/auth/register/route.ts` | high | Добавлен общий лимит 5 запросов с IP за 15 минут через PostgreSQL, HTTP 429 и Retry-After. Используется таблица `password_reset_attempts` из release-0017: перед публикацией нужно убедиться, что она применена. |
| Лимит входа действовал только для известного аккаунта | `app/api/v1/auth/login/route.ts` | high | Добавлен лимит 20 запросов с IP за 15 минут, в том числе для неизвестных email; лимит 8 неудачных попыток для существующего аккаунта сохранён. Разные IP для одного неизвестного email пока не агрегируются. |
| Реакции и ссылки на сообщения принимали ID из другого канала | `app/api/v1/channels/[channelId]/messages/route.ts` | high | Проверяется, что целевое сообщение существует именно в запрошенном канале и не удалено; мутации проверяют источник запроса. Два теста фиксируют запрет для недоступных сообщений. |
| Некоторые мутации обходили общую проверку источника запроса | `app/api/messages`, `app/api/v1/media`, `app/api/v1/channels/[channelId]/voice` | medium | Добавлена проверка `Origin`/Fetch Metadata до аутентификации; тест подтверждает HTTP 403 для cross-site POST. Остальные маршруты требуют отдельной проверки. |
| CSP отсутствовал в HTTP ответах | `next.config.ts` | high | Добавлены `object-src 'none'`, `base-uri 'self'`, `frame-ancestors 'none'`. Полная политика для скриптов/сетевых подключений требует браузерного теста Next.js, LiveKit и desktop. |
| Нет подтверждения Lighthouse/axe по требуемым шести маршрутам и пяти размерам | локальный браузер | medium | Playwright, axe-core и Lighthouse не настроены как проектные зависимости; нужны browser tooling и отдельный полный прогон. |
| Ограничение минимального окна Tauri 920×640 не позволяет компактную мобильную геометрию | `src-tauri/tauri.conf.json` | low | Оставлено: это desktop-клиент, мобильные размеры проверяются веб-версией. |
| Текст политики/условий и контактные реквизиты требуют подтверждения владельца | `app/privacy`, `app/terms` | medium | Страницы существуют. Владелец должен подтвердить юридические формулировки и рабочий адрес поддержки перед production-использованием. |

## Найденные работающие меры

- `next.config.ts` устанавливает `nosniff`, `X-Frame-Options: DENY`, частичную CSP, Referrer-Policy, Permissions-Policy, COOP и HSTS. На публичных страницах камера и микрофон теперь запрещены отдельной политикой.
- `app/layout.tsx` задаёт `viewport-fit=cover`; стили проекта уже используют `100dvh` во многих ключевых областях.
- `app/robots.ts` и `app/sitemap.ts` существуют.
- `app/privacy` и `app/terms` существуют; `/developers/docs` описывает токены и запросы.
- В отдельных API используются проверки членства и размера файлов; необходим отдельный полный тест IDOR/XSS по всем маршрутам.

## Вопросы владельцу

1. Подтверди стиль «ты» для всего интерфейса и публичных текстов (для главной он применён).
2. Для приватного локального QA предоставь доступ к тестовой БД и необходимые серверные переменные. Credentials уже сохранены в игнорируемом `.env.local`.
3. Подтверди юридические тексты и канал поддержки; сейчас футер ссылается на GitHub Issues.
4. Подтверди, что сейчас официально поддерживаются Web и Windows; другие desktop/mobile платформы пока не рекламируются.
5. Предоставь реальные метрики только если они нужны для публичного лендинга. До подтверждения числовые claims не следует показывать.

## Проверки

- `pnpm lint` — PASS.
- `pnpm exec tsc --noEmit` — PASS.
- `pnpm test` — PASS, 12 файлов / 74 теста.
- `pnpm exec next build --webpack` — PASS после всех правок. `pnpm build` (Turbopack) упал с внутренним panic в `static_sorted_file.rs`; Webpack-сборка завершилась успешно.
- HTTP smoke: `/`, `/login`, `/register`, `/download`, `/developers` отвечают 200; неизвестный URL — 404; `/app` без сессии — 307 на `/login?next=%2Fapp`. На `/login` и `/register` присутствует `noindex`; якоря `features`, `voice`, `communities` существуют. Авторизация возвращает 500 из-за отсутствия `DATABASE_URL`.
- Playwright viewport matrix, Lighthouse, axe и скриншоты — BLOCKED: бинарник Chromium отсутствует в окружении; production host недоступен из контейнера. Браузерные показатели и сравнение до/после не получены.
- Боевые данные не менялись. Тестовые credentials не попали в Git.

### Проверки прав доступа после продолжения

- `pnpm exec tsc --noEmit` — PASS.
- Отдельные тесты голосового токена и иерархии ролей — PASS, 4 теста.
- `pnpm lint` — PASS; `pnpm test` — PASS, 14 файлов / 78 тестов.
- `pnpm exec next build --webpack` — PASS; сборка завершена, предупреждение о deprecated Edge Runtime сохраняется.

### Продолжение 23 сентября

- Добавлены общие лимиты запросов на вход и регистрацию на основе PostgreSQL. Тесты проверяют 5 разрешённых попыток регистрации и 20 попыток входа; интеграционная проверка с реальной БД ожидает подключения тестовой среды.
- Добавлены частичная CSP для веба и Tauri и более строгая Permissions-Policy для публичных страниц. Подпись updater остаётся открытой до настройки отдельного выпуска и ключей.
- Исправлен IDOR при реакции и ссылках на другие сообщения канала. Без тестовой БД нельзя проверить живые права доступа и историю сообщений end-to-end.
- Закрыты пропуски проверки источника запросов в личных сообщениях, медиа и голосовых состояниях.
- Сканирование tracked файлов и добавленных строк истории Git по сигнатурам OpenAI/GitHub/AWS/Postgres URL и приватных ключей: 0 совпадений. Это ограниченная проверка типовых паттернов; специализированный сканер может найти иные форматы.


---

# SuperFlip / Store / Inventory audit — 25 сентября 2026

## Архитектура, найденная до реализации

- Backend: Next.js route handlers + PostgreSQL через `postgres` и Drizzle ORM; схема — `db/schema.ts`, SQL-миграции — `drizzle/`.
- Frontend: Next.js 16 / React 19; проект использует собственные CSS-слои и CSS Modules, без обязательной UI-библиотеки.
- Auth: `getCurrentUser()` уже используется в приватных API; mutation routes защищаются `isTrustedMutationRequest()`.
- SuperFlip уже существует: `/superflip`, `/api/superflip/status`, waitlist purchase route, `superflip_purchases` и capability checks в `lib/superflip.ts`. Реального payment provider в репозитории нет.
- Экономика уже существует: `user_wallets`, `coin_transactions`, transactional debit/credit в `lib/economy.ts`.
- Магазин/инвентарь уже существуют в базовом виде: `cosmetic_items`, `cosmetic_inventory`, `cosmetic_equipped`, `/api/v1/shop`, `components/personal-economy.tsx`, seed `config/cosmetics.json`.
- Equipped cosmetics уже попадают в профиль через `lib/presentation.ts`.
- Специализированного CDN/object-storage для store assets нет; user media хранится в `media_assets` и отдаётся приложением.
- Дизайн-система основана на CSS variables/tokens в `app/themes.css`, `app/runtime-theme.css`, `app/product-theme.css` и `app/mobile.css`.

## Решения по ТЗ

1. Не создавать параллельные `store_items/user_inventory/user_equipped_items`, а расширить существующий production-compatible cosmetics stack.
2. `cosmetic_equipped` переводится с семантики category на явный `slot`; primary key `(user_id, slot)` уже обеспечивает один активный предмет на слот.
3. В `cosmetic_items` добавляются slug/type/slot, animation/fallback metadata, bundle/active flags и timestamps.
4. Добавляется только недостающая таблица bundle entries и source для inventory.
5. В интерфейсе существующий внутренний balance показывается как Orbs; отдельный денежный checkout не выдумывается до появления billing provider.
6. SuperFlip UI можно расширять, но его CTA остаётся waitlist/status flow, потому что реального card billing в коде нет.
7. Animated store items реализуются с CSS animation и generic URL preview support/fallback; reduced-motion сохраняется.
